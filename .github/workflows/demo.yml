name: Demo workflow
on: workflow_dispatch
jobs: 
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello Worls actions
      - name: Print a few lines
        run: | 
          echo First line!
          echo Second line!
